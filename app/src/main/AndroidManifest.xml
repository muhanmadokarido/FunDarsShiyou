<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mk_kadish.fundarsshiyou">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".Game4.game4_HomeActivity"></activity>
        <activity android:name=".Game3.game3_HomeActivity" />
        <activity android:name=".Game2.game2_HomeActivity" />
        <activity android:name=".Game1.game1_HomeActivity" />
        <activity android:name=".indexActivity">
        </activity>
        <activity android:name=".Admin.userManagementActivity" />
        <activity android:name=".Admin.HomeActivity" />
        <activity android:name=".MainActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>